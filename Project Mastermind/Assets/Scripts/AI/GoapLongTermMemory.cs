using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GoapLongTermMemory : MonoBehaviour
{
    //MAYBE IMPLEMENT AS LONG TERM MEMORY
    //EITHER FROM ONGOING COMBAT IF ENTITY IS A BOSS
    //OR FROM PREVIOUS COMBAT PERFORMED IN THE GAME (LIKE SHADOW OF WAR)

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
